<h2>My planning</h2>
<%= link_to "sign out", destroy_user_session_path, method: :delete %>
<hr/>


<%= link_to "Add", new_event_path %>


<% if !@events.empty? %>

<!-- <% current_day = @events[0].event_date %> -->

<table border="1">

	<tr>
		<th>
			<%= @current_day %>
			<!-- current_day.to_formatted_s(:long_ordinal) -->
		</th>
	</tr>

	<% @events.each do |event| %>

	<% if event.event_date == @current_day %>

	<tr><td><%= event.title %><br/>
		<!-- .to_s(:time) -->
		<%= event.begti.strftime("%H:%M%P") %>-<%= event.endti.strftime("%H:%M%P") %><br/>
		<%= link_to "view", event %>


<!-- <td><a href="#child4">My clickable text</a></td> -->


	</td></tr>

	<% else %>
</table>
<hr/>
<table border="1">
	<% @current_day = event.event_date %>
	<tr>
		<th>
			<%= @current_day %>
		</th>
	</tr>
	<tr><td><%= event.title %><br/>
		<%= event.begti.strftime("%H:%M%P") %>-<%= event.endti.strftime("%H:%M%P") %><br/>
		<%= link_to "view", event %>
	</td></tr>
	<% end #if/else block %>

	<% end #loop end %>

</table>

<% else %>

<table>
	<tr><td>no events to show</td></tr>
</table>

<% end #if %>

